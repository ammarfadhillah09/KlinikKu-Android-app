<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F7FA">

    <!-- Header Bar -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:gravity="center_vertical"
        android:padding="16dp"
        android:background="#FFFFFF"
        android:elevation="4dp">

        <!-- Tombol Kembali -->
        <ImageView
            android:id="@+id/btnKembali"
            android:layout_width="36dp"
            android:layout_height="36dp"
            android:padding="6dp"
            android:src="@android:drawable/ic_menu_revert"
            app:tint="#2D3436"
            android:clickable="true"
            android:focusable="true"
            android:background="?android:attr/selectableItemBackgroundBorderless"
            android:contentDescription="Kembali" />

        <!-- Judul Halaman -->
        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="Antrean Pasien Hari Ini"
            android:textSize="18sp"
            android:textStyle="bold"
            android:textColor="#2D3436" />

        <!-- Badge Jumlah Pasien -->
        <TextView
            android:id="@+id/tvJumlahPasien"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:background="@android:drawable/dialog_holo_light_frame"
            android:paddingHorizontal="12dp"
            android:paddingVertical="4dp"
            android:text="0 pasien"
            android:textColor="#0984E3"
            android:textSize="12sp"
            android:textStyle="bold" />
    </LinearLayout>

    <!-- RecyclerView Daftar Pasien Antrean -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvAntreanPasien"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:clipToPadding="false"
        android:paddingTop="8dp"
        android:paddingBottom="16dp" />

    <!-- Empty State (tampil jika data kosong) -->
    <TextView
        android:id="@+id/tvEmptyState"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center"
        android:text="Belum ada pasien antrean hari ini"
        android:textColor="#B2BEC3"
        android:textSize="16sp"
        android:visibility="gone" />

</LinearLayout>
